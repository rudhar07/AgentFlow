# Core browser automation
playwright==1.49.1

# Optional LLM-vision mode (Claude). The deterministic agent runs without this,
# but it is required for `--mode llm`.
anthropic>=0.49.0

# Optional Gemini-vision mode (Google). Required for `--mode gemini`.
google-genai>=1.0.0

# Loads configuration / API keys from a local .env file
python-dotenv>=1.0.1

# Web dashboard (FastAPI server + SSE log/screenshot streaming). Used by
# `python dashboard.py` and by the Docker / Hugging Face Spaces deployment.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
